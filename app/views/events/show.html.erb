<% content_for :title, @event.title %>

<div class="row justify-content-center">
  <div class="col-md-8">

    <%# Photo de l'événement %>
    <% if @event.photo.attached? %>
      <%= image_tag @event.photo, class: "img-fluid rounded mb-4 w-100", style: "max-height: 400px; object-fit: cover;" %>
    <% end %>

    <%# Header de l'événement %>
    <div class="d-flex justify-content-between align-items-start mb-4">
      <div>
        <h1 class="mb-1"><%= @event.title %></h1>
        <p class="text-muted mb-0">Organisé par <strong><%= @event.user.email %></strong></p>
      </div>
      <span class="badge bg-warning text-dark fs-5"><%= @event.price %> €</span>
    </div>

    <%# Infos clés %>
    <div class="row g-3 mb-4">
      <div class="col-sm-6">
        <div class="card border-secondary h-100">
          <div class="card-body">
            <h6 class="text-muted mb-1">📅 Début</h6>
            <p class="mb-0 fw-semibold"><%= @event.start_date.strftime("%d/%m/%Y à %Hh%M") %></p>
          </div>
        </div>
      </div>
      <div class="col-sm-6">
        <div class="card border-secondary h-100">
          <div class="card-body">
            <h6 class="text-muted mb-1">🏁 Fin</h6>
            <p class="mb-0 fw-semibold"><%= @event.end_date.strftime("%d/%m/%Y à %Hh%M") %></p>
          </div>
        </div>
      </div>
      <div class="col-sm-6">
        <div class="card border-secondary h-100">
          <div class="card-body">
            <h6 class="text-muted mb-1">⏱️ Durée</h6>
            <p class="mb-0 fw-semibold"><%= @event.duration %> minutes</p>
          </div>
        </div>
      </div>
      <div class="col-sm-6">
        <div class="card border-secondary h-100">
          <div class="card-body">
            <h6 class="text-muted mb-1">📍 Lieu</h6>
            <p class="mb-0 fw-semibold"><%= @event.location %></p>
          </div>
        </div>
      </div>
    </div>

    <%# Participants %>
    <div class="card border-secondary mb-4">
      <div class="card-body d-flex justify-content-between align-items-center">
        <span class="text-muted">👥 Participants inscrits</span>
        <span class="badge bg-secondary fs-6"><%= @event.attendances.count %></span>
      </div>
    </div>

    <%# Description %>
    <div class="card border-secondary mb-4">
      <div class="card-body">
        <h5 class="card-title">Description</h5>
        <p class="card-text"><%= simple_format(@event.description) %></p>
      </div>
    </div>

    <%# Actions selon le rôle %>
    <% if user_signed_in? && @event.user == current_user %>
      <%# Organisateur %>
      <%= link_to "Mon espace événement", event_attendances_path(@event), class: "btn btn-warning" %>

    <% elsif user_signed_in? && @event.attendees.include?(current_user) %>
      <%# Déjà participant %>
      <span class="badge bg-success fs-6 p-2">✓ Vous participez à cet événement</span>

    <% elsif user_signed_in? %>
      <%# Connecté mais ni organisateur ni participant %>
      <%= link_to new_event_attendance_path(@event), class: "btn btn-primary" do %>
        <% if @event.is_free? %>
          Rejoindre l'événement (gratuit)
        <% else %>
          Rejoindre l'événement — <%= @event.price %> €
        <% end %>
      <% end %>

    <% else %>
      <%# Non connecté %>
      <%= link_to "Se connecter pour rejoindre", new_user_session_path, class: "btn btn-outline-primary" %>
    <% end %>

  </div>
</div>
